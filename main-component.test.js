const { it, describe } = require('@jest/globals')
const {
    renderDifficultPageComponent,
} = require('./difficulty-page-component')

describe("renderComponent", () => {
    describe('renderDifficultPageComponent()', () => {
        it('should render difficult page'), () => {
            const start = renderDifficultPageComponent();

        
        } 
    })
})